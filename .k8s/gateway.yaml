apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: feedi-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
  - hosts:
    - feedi.awes.one
    tls:
      mode: SIMPLE
      credentialName: awes-one-cert
    port:
      name: https
      number: 443
      protocol: HTTPS
  - hosts:
    - feedi.awes.one
    tls:
      httpsRedirect: true
    port:
      name: http
      number: 80
      protocol: HTTP
